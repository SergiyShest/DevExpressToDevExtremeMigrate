
@startuml

start

  :Call CollectInfoFromSourcePath();
    while (поиск всех директорий в которых есть файлы  "*grid*.cshtml") is (yes)
      :поиск файла основного файла ( чаще всего index.cshtml);
      :поиск файла с гридом;
      :поиск в файлах модели;
      :парсинг файла с гридом для получени информации колонках;      
      :поиск в исходном коде модели для получения информации о полях;      
      :поиск заголовка таблицы;
    endwhile
    :Сериализация результата в json;
    stop
@enduml
